{
  "description": "ai-tools-mcp session-start preflight: verify Google ADC (quick/deep_research, gemini_*) + the Perplexity Keychain entry (agent_research) are healthy before the user issues their first research query.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/preflight.sh\""
          }
        ]
      }
    ]
  }
}
