---
description: Run a Perplexity Sonar Pro deep-research query (fast, inline, multi-source with citations)
argument-hint: <research-query>
---

Use the `deep_research` MCP tool from the `ai-tools-mcp` server to investigate:

$ARGUMENTS

Synthesize the results into a clear answer. Preserve citations from the tool output verbatim — do not paraphrase or drop source attributions. Flag any conflicting sources rather than silently picking one.
